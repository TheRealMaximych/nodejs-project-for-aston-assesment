## 1. Entity, DataSource, migration

- [x] 1.1 Add `src/entities/transaction.ts` for table `transactions`: UUID `id`, `fromAccount` (`from_account` UUID FK to `bank_accounts.id` ON DELETE RESTRICT), `toAccount` (`to_account` UUID FK to `bank_accounts.id` ON DELETE RESTRICT), `amount` `numeric(20, 2)` as TypeScript `string` with a transformer that never uses IEEE `number`, `timestamp` timestamptz, `status` constrained to `Completed` | `Failed`; index `from_account` and `to_account`; register `Transaction` on `AppDataSource` next to `User` and `BankAccount` with `synchronize: false`; verify the entity field type of `amount` is `string` and DataSource `entities` includes `Transaction` with auto-sync off
- [x] 1.2 Add a versioned TypeORM migration (e.g. `migrations/1700000003000-CreateTransactions.ts`) that creates `transactions` with `numeric` amount (not float/double/real), FKs to `bank_accounts`, timestamp, and status CHECK (`Completed`/`Failed`); do not edit `InitialSchema`, `CreateUsers`, or `CreateBankAccounts`; verify the migration SQL uses `numeric` and that the first three migrations still do not create this table in the initial migration

## 2. Repository lock session

- [x] 2.1 Add `src/repositories/transfer-repository.ts` with `withLockedAccounts(fromAccountId, toAccountId, work)` that starts a TypeORM query-runner transaction, locks both account rows with `SELECT FOR UPDATE` (or `setLock("pessimistic_write")`) in UUID-sorted order, passes `{ from, to, complete }` to `work` (`from`/`to` labeled by request ids, `balance`/`amount` strings), commits on resolve and rolls back on throw; `complete(amount)` MUST debit source, credit destination, and insert a `Completed` Transaction in that same transaction and return `{ transactionId, status: "Completed" }`; verify HTTP and service files do not import TypeORM `DataSource` / `Repository` / QueryBuilder / QueryRunner

## 3. Transfer service and tests

- [x] 3.1 Add `src/services/transfer-money.ts`: Zod-validate `fromAccount`/`toAccount` as UUID and `amount` as a positive decimal string (max 2 fraction digits, not a JSON number); normalize amount to two fraction digits; `from === to` → `SameAccountTransferError` without calling the repository; inside `withLockedAccounts` apply check order `from` missing → `NotFoundError`, foreign `from.userId` → `ForbiddenError`, `to` missing → `NotFoundError`, currency mismatch → `CurrencyMismatchError`, insufficient minor-unit balance → `InsufficientFundsError`, else `complete(normalizedAmount)`; compare money with `bigint` cents (no `Number`/`parseFloat`); log exactly `Transfer completed: from=<id> to=<id> amount=<amount>` on success and `Transfer failed: from=<id>, reason=<reason>` before rethrow (`insufficient funds`, `currency mismatch`, `same account`, `forbidden`, `not found`); return `{ transactionId, status: "Completed" }`; injectable deps; verify the service does not import TypeORM or `process.env`
- [x] 3.2 Add `src/services/transfer-money.test.ts` with a mocked `withLockedAccounts`: own→own success calls `complete` and returns only `{ transactionId, status: "Completed" }` with a string amount in the completed log; own→foreign same currency calls `complete`; insufficient funds → `InsufficientFundsError` with message `Insufficient funds`, `complete` unused, log `reason=insufficient funds`; foreign from → `ForbiddenError`; different currency → `CurrencyMismatchError`; from = to → `SameAccountTransferError` and session unused; missing from or to → `NotFoundError`; JSON number / non-positive amount → `ValidationError`; logs contain no password/hash/JWT/`accessToken`; verify `npm test` includes these cases

## 4. HTTP

- [x] 4.1 Add `src/http/routes/transactions.ts` mounted at `/transactions` in `app.ts` before `notFound`: `POST /` uses `requireAuth`; handler calls `transferMoney(req.userId, req.body)` and responds 201 with `{ transactionId, status }`; missing `req.userId` → `UnauthorizedError()`; no Zod, TypeORM, `process.env`, deposit, or `GET /:accountId` history route; verify `src/http` has no TypeORM and does not coerce `amount` to a JSON number
- [x] 4.2 Replace the `POST /transactions returns 404` assertion in `src/http/middleware/not-found.test.ts` with `GET /transactions/:accountId`; add a test that `POST /transactions` without `Authorization` returns 401 `{ error, statusCode }` (not 404); verify `npm test` still covers unknown-path 404 and the missing-Bearer 401 case

## 5. Build and layering

- [x] 5.1 Grep `src/http` and `src/services` for `process.env`, TypeORM imports, `parseFloat`/`Number(` on amount or balance, deposit routes, and `GET` history under `/transactions`; verify those greps are empty (the transfer service may import Zod and lazy-load the transfer repository)
- [x] 5.2 Run `npm test` and `npm run build` and verify both exit 0 with `strict` compilation
